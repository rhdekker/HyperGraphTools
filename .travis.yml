# Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia
os:
  - linux
  - osx
#  - windows % The language 'julia' is currently unsupported on the Windows Build Environment.
julia:
  - nightly
matrix:
  allow_failures:
    - julia: nightly
notifications:
  email: false
script:
  - julia -e 'import Pkg;Pkg.clone(pwd()*"/HRGVisualization"); Pkg.build("HRGVisualization"); Pkg.test("HRGVisualization"; coverage=true)'
  - julia -e 'import Pkg;Pkg.clone(pwd()*"/SimpleTAGMLTokenizer"); Pkg.build("SimpleTAGMLTokenizer"); Pkg.test
  - julia -e 'import Pkg;Pkg.clone(pwd()*"/HRGParser"); Pkg.build("HRGParser"); Pkg.test("HRGParser"; coverage=true)'
codecov: true
coveralls: true